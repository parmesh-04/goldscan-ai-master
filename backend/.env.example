# GoldScan AI Backend Configuration

# Gemini API Key (Required for vision analysis)
GEMINI_API_KEY=your_gemini_api_key_here

# Production Mode (false = use safe mocks when API fails)
PRODUCTION_MODE=false
